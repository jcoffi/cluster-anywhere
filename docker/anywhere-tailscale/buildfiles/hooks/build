#!/bin/bash

# Set image type based on build argument
docker build --cache-from=rayproject/ray:latest-py38-$DOCKER_TAG . --ulimit memlock=-1 -t jcoffi/cluster-anywhere:$DOCKER_TAG --build-arg IMAGETYPE=$DOCKER_TAG